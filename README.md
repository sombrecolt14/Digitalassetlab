
  # Design System Setup

  This is a code bundle for Design System Setup. The original project is available at https://www.figma.com/design/zM1phW5Nu6nbYcsi9kj30d/Design-System-Setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  